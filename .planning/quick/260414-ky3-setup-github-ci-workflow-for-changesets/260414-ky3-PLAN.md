# Quick task 260414-ky3: Setup GitHub CI workflow for Changesets

## Tasks

### Task 1 — CI and Changesets release workflows

- **files:** `.github/workflows/ci.yml`, `.github/workflows/release.yml`
- **action:** Add a CI workflow (typecheck, unit tests, compile) on `pull_request` and pushes to `main`. Add a `release` workflow on pushes to `main` that runs `changesets/action@v1` with `version: npm run changeset:version`, `contents` + `pull-requests` permissions, Node 22, and `npm ci`. Omit `publish` so npm credentials are not required until you opt in; document how to add `publish` + `NPM_TOKEN` in the quick summary.
- **verify:** Workflow YAML is valid; local `npm run check-types && npm run test:unit && npm run compile` passes (unchanged).
- **done:** [x]
