---
phase: 04-cleanup-review-ui
plan: 2
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/git/cleanupReviewPick.test.ts
  - package.json
autonomous: true
requirements:
  - UXP-01
  - UXP-02
  - UXP-03
user_setup: []
must_haves:
  truths:
    - "Automated tests lock sort order and merge detail baseline interpolation."
    - "Repo-wide grep confirms Phase 4 surfaces do not introduce deleteBranch outside type stubs."
    - "Contributed command title/category unchanged or still compliant with no bulk-delete implication (UXP-02)."
  artifacts:
    - path: src/git/cleanupReviewPick.test.ts
      provides: "Vitest cases for sort order and detail string contents."
  key_links:
    - from: src/git/cleanupReviewPick.test.ts
      to: src/git/cleanupReviewPick.ts
      via: "imports functions under test"
---

<objective>
Harden Phase 4 with **Vitest** coverage for pure helpers and **grep-based** regression gates for **UXP-01** (no delete), **UXP-02** (no misleading delete-all affordance in contributed command metadata), **UXP-03** (baseline string appears in extension QuickPick options). Depends on **04-01** artifacts existing.
Purpose: Make requirements verifiable in CI without Extension Host for sort/copy logic.
Output: Unit test file + minimal manifest copy check if needed.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-cleanup-review-ui/04-VALIDATION.md
@src/git/cleanupReviewPick.ts
@src/extension.ts
@package.json
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / tests ↔ production | Tests validate ordering/copy only; they do not invoke Git or `vscode`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Elevation of privilege | False confidence from green tests | mitigate | Grep gate still asserts **no** `deleteBranch(` in `src/extension.ts`; tests do not replace manual UAT for QuickPick dismiss. |
| T-04-07 | Misleading trust | `package.json` command title | mitigate | Keep command title **Cleanup Branches** / category **Git**; do not add a second command titled **Delete All Branches** or equivalent (**UXP-02**). |
| T-04-08 | Information disclosure | Test fixtures | accept | Fixture branch names are synthetic strings only. |

Command injection / secrets in tests: **N/A**.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Vitest for sort order and mergeDetailLine baseline text</name>
  <files>src/git/cleanupReviewPick.test.ts</files>
  <read_first>
    - `src/git/cleanupReviewPick.ts` exported function signatures
    - `vitest.config.mts` (`include: ['src/**/*.test.ts']`)
    - `src/git/cleanupRun.ts` for constructing minimal `CleanupCandidateRow` objects (only `branch.name` and `merge` required)
  </read_first>
  <action>
    Create **`src/git/cleanupReviewPick.test.ts`** using **`vitest`**.

    1. **`mergeDetailLine`**: For **`baselineDisplayLabel === 'origin/main'`**, assert outputs equal exactly:
       - merged → **`Merged into baseline: origin/main.`**
       - not_merged → **`Not merged into baseline: origin/main.`**
       - unknown → **`Could not verify merge into baseline: origin/main.`**

    2. **`sortCandidatesForReview`**: Given rows **`feature`** not_merged, **`hotfix`** merged, **`zzz`** merged, **`alpha`** unknown — expect order **`hotfix`, `zzz`, `feature`, `alpha`** (merged group sorted A–Z, then not_merged, then unknown).

    Use minimal objects: **`{ branch: { name: string, ...optional }, merge: ... }`** as **`CleanupCandidateRow`** — satisfy TypeScript with **`as CleanupCandidateRow`** only if needed and only with required fields populated.

    Do **not** import **`vscode`**.
  </action>
  <acceptance_criteria>
    - `npm run test:unit -- src/git/cleanupReviewPick.test.ts` exits **0**.
    - `rg "describe\\(|it\\(" src/git/cleanupReviewPick.test.ts` exits **0** (at least one test block).
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/git/cleanupReviewPick.test.ts</automated>
  </verify>
  <done>Unit tests cover mergeDetailLine literals and sort order.</done>
</task>

<task type="auto">
  <name>Task 2: Regression gates and full compile/test sweep</name>
  <files>package.json</files>
  <read_first>
    - `package.json` `contributes.commands` for `branchCleaner.cleanupBranches`
    - `src/extension.ts` QuickPick **`title`** / **`placeHolder`** pattern from plan **04-01**
  </read_first>
  <action>
    1. **If and only if** `package.json` lacks an aggregate **`test`** script, add **`"test": "npm run test:unit"`** to **`scripts`** (preserve existing JSON formatting). If **`test`** already exists, **do not** change **`package.json`**.

    2. No changes to command **`title`** / **`category`** except if you find a string implying bulk delete (unlikely) — in that case fix to remain **Cleanup Branches** / **Git** per EXT-01 style.

    3. Run full **`npm run test:unit`** and **`npm run compile`** from repo root (executor runs these; acceptance checks exit codes).

    Document in SUMMARY (later) if **`test`** script was added.
  </action>
  <acceptance_criteria>
    - `rg "deleteBranch\\(" src/extension.ts` exits **1**.
    - `rg "Branch Cleaner — baseline:" src/extension.ts` exits **0** (**UXP-03** — baseline in QuickPick title).
    - `rg "\"title\": \"Delete All" package.json` exits **1** (sanity: no delete-all command title).
    - `npm run test:unit` exits **0**.
    - `npm run compile` exits **0**.
  </acceptance_criteria>
  <verify>
    <automated>rg "deleteBranch\\(" src/extension.ts ; test $? -eq 1 &amp;&amp; rg "Branch Cleaner — baseline:" src/extension.ts &amp;&amp; rg "\"title\": \"Delete All" package.json ; test $? -eq 1 &amp;&amp; npm run test:unit &amp;&amp; npm run compile</automated>
  </verify>
  <done>Grep gates pass; full test + compile green.</done>
</task>

</tasks>

<verification>
- Full unit suite + compile green.
- Grep gates above pass.
</verification>

<success_criteria>
- Sort and detail strings are regression-tested.
- CI-style commands confirm no accidental **`deleteBranch`** wiring in **`extension.ts`** and baseline remains visible in picker chrome source.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cleanup-review-ui/04-02-SUMMARY.md`
</output>
