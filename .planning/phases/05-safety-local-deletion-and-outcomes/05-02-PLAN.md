---
phase: 05-safety-local-deletion-and-outcomes
plan: 2
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/git/localBranchDeletion.test.ts
  - package.json
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
user_setup: []
must_haves:
  truths:
    - "Unit tests lock failures-first report ordering and sequential delete behavior with a stub Repository."
    - "Grep gates show deleteBranch is confined to implementation files and no push in extension handler."
  artifacts:
    - path: src/git/localBranchDeletion.test.ts
      provides: "Vitest coverage for Phase 5 pure helpers."
  key_links:
    - from: src/git/localBranchDeletion.test.ts
      to: src/git/localBranchDeletion.ts
      via: "imports under test"
---

<objective>
Add **Vitest** coverage and **grep regression gates** for Phase 5: **SAFE-02** (no **`push`** in **`src/extension.ts`**; **`deleteBranch`** only in expected **`src/`** files), **SAFE-03** (report line ordering), and guardrails that **SAFE-01** helpers behave (**risk confirmation predicate**, **per-branch force** path in stub).
</objective>

<context>
@.planning/phases/05-safety-local-deletion-and-outcomes/05-VALIDATION.md
@src/git/localBranchDeletion.ts
@src/extension.ts
@package.json
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests vs production | Stub `Repository` does not touch disk Git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | False confidence | Green tests without grep | mitigate | **`rg`** gates in Task 2 still enforce no **`.push(`** in **`src/extension.ts`**. |

**N/A:** Remote deletion (not testable without integration).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Vitest for localBranchDeletion helpers</name>
  <files>src/git/localBranchDeletion.test.ts</files>
  <read_first>
    - `src/git/localBranchDeletion.ts`
    - `vitest.config.mts`
    - `src/git/cleanupRun.ts` for **`CleanupCandidateRow`** shape
  </read_first>
  <action>
    Create **`src/git/localBranchDeletion.test.ts`** (Vitest, no **`vscode`** import).

    1. **`formatDeletionReportLines`**: Given outcomes **`[{ ok: false, branchName: 'a', errorMessage: 'x' }, { ok: true, branchName: 'b' }]`** — joined line order must list **`a`** before **`b`**; failure line contains **`failed — x`**; success line equals **`b: deleted`**.

    2. **`selectionNeedsRiskConfirmation`**: **`false`** when all **`merged`**; **`true`** when any **`not_merged`** or **`unknown`**.

    3. **`deleteLocalBranchesSequential`**: Stub **`Repository`** with **`deleteBranch`** that **rejects** on second call — expect two outcomes, first **`ok: true`**, second **`ok: false`** with message; third op still runs if provided (partial batch continuation).

    4. **`deleteLocalBranchesSequential`**: When **`force`** flags are passed through, assert stub received **`(name, force)`** matching each operation (spy / mock implementation).
  </action>
  <acceptance_criteria>
    - `npm run test:unit -- src/git/localBranchDeletion.test.ts` exits **0**.
    - `rg "describe\\(|it\\(" src/git/localBranchDeletion.test.ts` exits **0**.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- src/git/localBranchDeletion.test.ts</automated>
  </verify>
  <done>Unit tests cover report order, risk predicate, and sequential deletes.</done>
</task>

<task type="auto">
  <name>Task 2: Regression grep + full unit run</name>
  <files>package.json</files>
  <read_first>
    - `src/extension.ts` for **`showWarningMessage`** / **`showInformationMessage`** usage
    - `package.json` scripts
  </read_first>
  <action>
    1. **`rg "\\.push\\(" src/extension.ts`** must exit **1** (**SAFE-02** — no push in command handler).

    2. **deleteBranch scope:** `rg "deleteBranch\\(" src/` lists only **`src/extension.ts`** and **`src/git/localBranchDeletion.ts`** and **`src/types/git.d.ts`** (type stub). If matches appear elsewhere, narrow implementation until only these paths remain (or **`extension.ts`** delegates entirely to **`localBranchDeletion.ts`** — then **`extension.ts`** may have **zero** `deleteBranch` literals).

    3. Run **`npm run test:unit`** and **`npm run compile`** from repo root; both exit **0**.

    4. If **`package.json`** lacks aggregate **`"test": "npm run test:unit"`**, add it (preserve JSON formatting); otherwise leave unchanged.
  </action>
  <acceptance_criteria>
    - `rg "\\.push\\(" src/extension.ts` exits **1**.
    - `npm run test:unit` exits **0**.
    - `npm run compile` exits **0**.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit && npm run compile</automated>
  </verify>
  <done>CI-style gates and full suite green.</done>
</task>

</tasks>

## PLANNING COMPLETE
