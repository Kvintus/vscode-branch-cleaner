---
phase: 03-domain-candidates-baseline-merge
plan: 2
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - src/git/baselineResolver.ts
  - src/git/mergeClassification.ts
  - src/git/cleanupRun.ts
  - src/domain/mergeSemantics.ts
  - src/domain/mergeSemantics.test.ts
  - src/git/mergeClassification.test.ts
  - src/extension.ts
autonomous: true
requirements:
  - DOM-03
  - DOM-04
  - QUAL-01
must_haves:
  truths:
    - "resolveBaselineForRun(repository) calls repository.getRefs (remote: true) once per successful run and feeds the snapshot into domain resolveBaselineFromOriginRemoteRefs (D-04 wiring for DOM-03)."
    - "buildCleanupRunPlan(repository, ...) returns one ResolvedBaseline shared by every candidate row’s merge state for that run (DOM-04)."
    - "Merged vs not merged uses getMergeBase(baselineRef, candidateRef) and ancestor rule: merged iff merge base OID equals candidate tip OID; undefined merge base maps to unknown (D-06)."
    - "When getMergeBase rejects or throws for a row, that row’s merge state is unknown (not silently not_merged) (D-06)."
    - "When baseline resolution fails (BaselineResolutionError), orchestration does not fabricate merge labels per candidate — contract surfaces failure to caller (D-04 step 4 + DOM-03)."
    - "Extension Cleanup Branches command shows an information message that includes resolved baseline displayLabel and candidate count OR explicit baseline-failure message text — no QuickPick (Phase 4 deferred)."
  artifacts:
    - path: src/git/baselineResolver.ts
      provides: getRefs snapshot → domain types; resolveBaselineForRun
    - path: src/domain/mergeSemantics.ts
      provides: mergeLabelFromMergeBase(candidateTipOid, mergeBaseOid) pure mapper
    - path: src/git/mergeClassification.ts
      provides: classifyMergeForCandidate(repository, baselineRef, candidateBranch) using full ref names
    - path: src/git/cleanupRun.ts
      provides: buildCleanupRunPlan (single baseline + per-branch merge state)
    - path: src/git/mergeClassification.test.ts
      provides: mocked getMergeBase merged/not_merged/unknown matrix
    - path: src/domain/mergeSemantics.test.ts
      provides: pure merge label unit tests
    - path: src/extension.ts
      provides: calls buildCleanupRunPlan and surfaces summary message
  key_links:
    - from: src/git/baselineResolver.ts
      to: src/domain/baseline.ts
      via: imports resolveBaselineFromOriginRemoteRefs / error type from plan 03-01
    - from: src/git/cleanupRun.ts
      to: src/domain/candidates.ts
      via: listCleanupCandidates(repository.state.HEAD?.name, branches from listLocalBranches)
    - from: src/git/cleanupRun.ts
      to: src/git/mergeClassification.ts
      via: for each candidate, same baselineRef string
---

<objective>
Wire **vscode.git** `Repository.getRefs` into `resolveBaselineForRun`, implement **merge classification** via `getMergeBase` with ancestor semantics, export **one orchestration entry** (`buildCleanupRunPlan`) that returns a per-run `ResolvedBaseline` plus per-candidate merge labels, cover merge mapping with Vitest mocks, and minimally integrate into the existing **Cleanup Branches** command without QuickPick.

Purpose: Completes **DOM-03** (live ref resolution), **DOM-04** (single baseline for all rows; unknown on API failure), and extends **QUAL-01** to mocked Git API behavior.

Output: `src/git/baselineResolver.ts`, `src/git/mergeClassification.ts`, `src/git/cleanupRun.ts`, `src/domain/mergeSemantics.ts`, associated `*.test.ts`, and updated `src/extension.ts` messaging.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-domain-candidates-baseline-merge/03-CONTEXT.md
@.planning/phases/03-domain-candidates-baseline-merge/03-RESEARCH.md
@src/types/git.d.ts
@src/git/branches.ts
@src/git/api.ts
@src/git/repositoryPicker.ts
@src/extension.ts
@src/domain/baseline.ts
@src/domain/candidates.ts
@src/domain/types.ts

**Locked decisions:** D-04 full resolution path using `getRefs`; D-06 `getMergeBase` + ancestor merged definition; D-07 squash/rebase stance = document in code comment only; DOM-04 single baseline for all merge checks in one `buildCleanupRunPlan` call.

**getRefs query (executor must match RESEARCH):** call `repository.getRefs({ remote: true })` unless typings require additional fields — if API demands pattern, use `pattern: 'refs/remotes/origin/*'` **plus** include `refs/remotes/origin/HEAD` (verify against `RefQuery` in `git.d.ts`; adjust to achieve HEAD + all origin remote heads in one or two calls without raw Git CLI).

**Ref strings:** pass **full** `refs/heads/<name>` for local tips from `listLocalBranches` / `getBranches` when available; baseline ref is `ResolvedBaseline.ref` from domain resolution.

<interfaces>
From `src/types/git.d.ts` (executor must use these signatures):

```typescript
getRefs(query: RefQuery, cancellationToken?: CancellationToken): Promise<Ref[]>;
getMergeBase(ref1: string, ref2: string): Promise<string | undefined>;
```

`Ref`: `type`, optional `name`, `commit`, optional `remote`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vscode.git API | Untrusted repo state could be hostile-shaped; still no shell concatenation — API only. |
| Extension command | User-triggered read-only classification + message; no delete in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | ref strings from Git | mitigate | Use refs returned by `getRefs`/`getBranches` only; never interpolate user text into CLI (no CLI). |
| T-03-02-02 | Elevation | extension host | accept | Read-only Git APIs; no privilege expansion beyond VS Code Git extension. |
| T-03-02-03 | Denial of service | pathological repo size | mitigate | Single getRefs + bounded branch list from Phase 2 helper; no loops over unbounded network. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: getRefs adapter, resolveBaselineForRun, mergeSemantics helper</name>
  <files>src/git/baselineResolver.ts, src/domain/mergeSemantics.ts, src/domain/mergeSemantics.test.ts</files>
  <read_first>
    - src/types/git.d.ts
    - src/domain/baseline.ts
    - src/domain/types.ts
    - .planning/phases/03-domain-candidates-baseline-merge/03-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "resolveBaselineForRun" src/git/baselineResolver.ts` matches exported async function taking `Repository`.
    - `rg -n "getRefs" src/git/baselineResolver.ts` matches call with `{ remote: true` OR documented equivalent that still retrieves `refs/remotes/origin/HEAD` + heads.
    - `rg -n "resolveBaselineFromOriginRemoteRefs|BaselineResolutionError" src/git/baselineResolver.ts` matches import from `../domain/baseline` or `../domain/types` (path as implemented).
    - `rg -n "mergeLabelFromMergeBase" src/domain/mergeSemantics.ts` matches exported pure function implementing D-06 mapping.
    - `rg -n "mergeLabelFromMergeBase" src/domain/mergeSemantics.test.ts` matches tests calling the helper and `npm run test:unit` passes.
    - `npm run check-types` exits 0.
  </acceptance_criteria>
  <action>
    Add `src/git/baselineResolver.ts` exporting `async function resolveBaselineForRun(repository: Repository): Promise<ResolvedBaseline>` that: (1) calls `repository.getRefs` per RESEARCH; (2) maps `Ref[]` into the plain shape expected by `resolveBaselineFromOriginRemoteRefs` from **03-01** (include `type`, `name`, `remote`, `commit` fields needed to detect `RemoteHead` / symref behavior — implement small normalizers in this file, **no** `import 'vscode'` in domain files). On domain `BaselineResolutionError`, rethrow or return rejected Promise with same error type. Add `src/domain/mergeSemantics.ts` with `export function mergeLabelFromMergeBase(candidateTipOid: string, mergeBaseOid: string | undefined): 'merged' | 'not_merged' | 'unknown'` where `unknown` when `mergeBaseOid` is undefined, `merged` when `mergeBaseOid === candidateTipOid`, else `not_merged`. In `mergeSemantics.test.ts`, add a Vitest suite that **imports and calls** `mergeLabelFromMergeBase` in **at least three** cases (merged, not_merged, unknown) — use `describe`/`it` or `test.each`, whichever you prefer. **Symref handling:** if `getRefs` returns `origin/HEAD` as direct commit or indirect, normalize to the resolved `refs/remotes/origin/<branch>` target before calling domain resolver (document behavior in file header).
  </action>
  <verify>
    <automated>cd /Users/jakobrolik/programming/momentum/gamma/vscode-branch-cleaner && npm run test:unit && npm run check-types</automated>
  </verify>
  <done>Baseline adapter compiles; pure merge semantics tested.</done>
</task>

<task type="auto">
  <name>Task 2: mergeClassification + buildCleanupRunPlan + mocks + extension wiring</name>
  <files>src/git/mergeClassification.ts, src/git/cleanupRun.ts, src/git/mergeClassification.test.ts, src/extension.ts</files>
  <read_first>
    - src/git/branches.ts
    - src/domain/candidates.ts
    - src/domain/types.ts
    - src/git/baselineResolver.ts
    - src/extension.ts
  </read_first>
  <acceptance_criteria>
    - `rg -n "getMergeBase" src/git/mergeClassification.ts` matches.
    - `rg -n "buildCleanupRunPlan" src/git/cleanupRun.ts` matches exported async function.
    - `rg -n "vi\\.fn|vitest" src/git/mergeClassification.test.ts` matches mock of getMergeBase.
    - Test file asserts **unknown** when mock `getMergeBase` returns `undefined` or throws.
    - Test file asserts **same** `baseline.ref` reused for two candidates in one `buildCleanupRunPlan` call (grep for two expectations against one variable).
    - `rg -n "buildCleanupRunPlan|resolveBaselineForRun" src/extension.ts` matches — command uses orchestration.
    - `rg -n "showInformationMessage" src/extension.ts` matches and message includes substring `Branch Cleaner` (existing pattern).
    - `npm run test:unit` and `npm run compile` exit 0.
  </acceptance_criteria>
  <action>
    Implement `src/git/mergeClassification.ts` exporting `async function classifyMergeForCandidate(repository: Repository, baselineRef: string, candidate: LocalBranchSummary): Promise<'merged' | 'not_merged' | 'unknown'>`: build **full local head ref** `refs/heads/${candidate.name}` for `getMergeBase(baselineRef, candidateRef)` (prefer full refnames). Let `mergeBaseOid = await repository.getMergeBase(baselineRef, candidateRef)`. Let `candidateTipOid = candidate.commit` (OID string); if `candidateTipOid` is missing/empty, return **`unknown`** without guessing from `mergeBaseOid` (D-06). Map `(candidateTipOid, mergeBaseOid)` through `mergeLabelFromMergeBase` from `src/domain/mergeSemantics.ts`. Catch errors from `getMergeBase` → **`unknown`**. Implement `src/git/cleanupRun.ts` exporting `buildCleanupRunPlan(repository: Repository, token?: vscode.CancellationToken)` returning `{ baseline: ResolvedBaseline; candidates: Array<{ branch: LocalBranchSummary; merge: 'merged' | 'not_merged' | 'unknown' }> }` where: (1) `listLocalBranches(repository, token)`; (2) `currentHeadName = repository.state.HEAD?.name`; (3) `listCleanupCandidates` from domain; (4) `baseline = await resolveBaselineForRun(repository)` — if baseline resolution throws `BaselineResolutionError`, propagate to caller; (5) for **each** candidate, `merge = await classifyMergeForCandidate(repository, baseline.ref, branch)` using the **same** `baseline.ref`. Add `mergeClassification.test.ts` with a fake repository object `{ getRefs, getMergeBase, state: { HEAD: { name: 'main' } } }` typed as `unknown as Repository` if needed, asserting DOM-04 single-baseline behavior and unknown path. Update `src/extension.ts` command: after `listLocalBranches` today, call `buildCleanupRunPlan`; on success, `showInformationMessage` with `baseline.displayLabel`, candidate count, and counts of merged vs not_merged vs unknown (simple template string); on `BaselineResolutionError`, `showErrorMessage` with user-facing text prefix `Branch Cleaner:` and body mentioning cannot resolve default branch. **No QuickPick** UI.
  </action>
  <verify>
    <automated>cd /Users/jakobrolik/programming/momentum/gamma/vscode-branch-cleaner && npm run test:unit && npm run compile</automated>
  </verify>
  <done>End-to-end read path + domain orchestration compiles; tests cover merge unknown + shared baseline; command surfaces summary.</done>
</task>

</tasks>

<verification>
- `npm run test:unit` exits 0.
- `npm run compile` exits 0.
- Manual smoke (optional): run **Git: Cleanup Branches** in a sample repo and confirm message shows baseline label.
</verification>

<success_criteria>
DOM-03 satisfied via `getRefs` + domain resolution; DOM-04 satisfied via single baseline ref for all merge classifications with unknown on failures; QUAL-01 extended to mocked `getMergeBase` paths; extension shows non-interactive summary only.
</success_criteria>

<output>
After completion, create `.planning/phases/03-domain-candidates-baseline-merge/03-02-SUMMARY.md` per execute-plan convention if required.
</output>
