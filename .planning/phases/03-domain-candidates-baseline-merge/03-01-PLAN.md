---
phase: 03-domain-candidates-baseline-merge
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - src/domain/types.ts
  - src/domain/candidates.ts
  - src/domain/baseline.ts
  - src/domain/candidates.test.ts
  - src/domain/baseline.test.ts
autonomous: true
requirements:
  - DOM-01
  - DOM-02
  - DOM-03
  - QUAL-01
must_haves:
  truths:
    - "Vitest runs via npm run test:unit with zero failing tests after this plan."
    - "Current branch short name never appears in candidate lists produced by listCleanupCandidates in fixtures (per D-01/D-02, DOM-02)."
    - "Detached HEAD (no currentHeadName): no synthetic current name; only named branches passing abandonment are candidates (D-02)."
    - "Fallback probe order for baseline when origin/HEAD is missing or unusable is exactly refs/remotes/origin/main, then refs/remotes/origin/master, then lexicographically sorted refs/remotes/origin/* excluding HEAD (D-04 / DOM-03 ordering slice)."
    - "Primary baseline choice prefers symref refs/remotes/origin/HEAD resolving to refs/remotes/origin/<branch> under origin when present (D-04 primary)."
    - "Structured BaselineResolutionError (or equivalent exported type) exists for no usable remote baseline; no silent local main fallback (D-04 step 4)."
  artifacts:
    - path: vitest.config.ts
      provides: Vitest node environment and test include globs
    - path: src/domain/types.ts
      provides: ResolvedBaseline, BaselineResolutionError, RemoteRefLike DTOs (no vscode import)
    - path: src/domain/candidates.ts
      provides: isAbandonedCandidate, listCleanupCandidates
    - path: src/domain/baseline.ts
      provides: orderBaselineFallbacks / resolveBaselineFromOriginRemoteRefs pure helpers
    - path: src/domain/candidates.test.ts
      provides: DOM-01/DOM-02 fixture coverage
    - path: src/domain/baseline.test.ts
      provides: DOM-03 ordering + primary symref fixtures
  key_links:
    - from: src/domain/candidates.ts
      to: src/git/branches.ts LocalBranchSummary shape
      via: duplicate DTO fields or narrow type imports only if tsc allows without bundling vscode into vitest
      pattern: "listCleanupCandidates\\(.*currentHeadName"
    - from: src/domain/baseline.ts
      to: "03-02 orchestration"
      via: same ResolvedBaseline contract consumed by resolveBaselineForRun in plan 2
---

<objective>
Add Vitest for Node-isolated unit tests, introduce `src/domain/` pure functions for gitcleaner-style cleanup candidates (excluding current HEAD) and for baseline fallback **ordering / primary symref resolution from ref snapshots**, plus exported types for a successful `ResolvedBaseline` and structured failure when no remote baseline exists.

Purpose: Satisfies **DOM-01**, **DOM-02**, the **ordering and primary-choice** slice of **DOM-03** (full `getRefs` wiring lands in 03-02), and **QUAL-01** for predicates + baseline ladder tests.

Output: `vitest.config.ts`, `npm run test:unit`, and `src/domain/**` with passing `*.test.ts` files.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-domain-candidates-baseline-merge/03-CONTEXT.md
@.planning/phases/03-domain-candidates-baseline-merge/03-RESEARCH.md
@src/git/branches.ts
@CLAUDE.md
@package.json

**Locked decisions implemented here:** D-01, D-02, D-04 primary + fallback ordering (steps 1–3) and typed failure object for step 4; D-08 (`src/domain/` pure module); D-09 (Vitest `^4.1.4`).

**Explicitly deferred:** D-05 (settings), CFG-01, QuickPick (Phase 4), `Repository.getRefs` adapter (03-02).

**Abandonment predicate (D-01) — encode verbatim for executors:** treat a local as abandoned iff `upstream` is **undefined** OR `upstream.commit` is **undefined** / empty **or** `upstream.remote` is not exactly `origin` (optional: document as “invalid tracking for default remote v1”); refinements for real “gone” runtime fields stay compatible with fixtures in `candidates.test.ts`.
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supply chain (npm) | New devDependency `vitest`; must pin `^4.1.4` per research and keep lockfile committed. |
| Local repo data | Domain consumes Git-shaped strings from fixtures/tests only in this plan; no live delete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | vitest/npm supply chain | mitigate | Pin `vitest@^4.1.4` in package.json; `npm install`; commit lockfile; prefer `npm ci` in CI. |
| T-03-01-02 | Information disclosure | test fixtures | accept | Fixtures use fake branch names only; no user paths required. |
| T-03-01-03 | Denial of service | mis-test infinite loop | mitigate | Pure functions only; tests must complete in <10s total. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Vitest devDependency, config, and test:unit script</name>
  <files>package.json, package-lock.json, vitest.config.ts</files>
  <read_first>
    - .planning/phases/03-domain-candidates-baseline-merge/03-RESEARCH.md
    - package.json
    - .planning/phases/01-extension-scaffold-activation-and-packaging/01-PLAN.md
  </read_first>
  <acceptance_criteria>
    - `rg -n '"vitest"' package.json` matches a devDependency with range `^4.1.4` (caret allowed) or exact `4.1.4`.
    - `rg -n 'test:unit' package.json` matches a scripts entry running vitest (e.g. `vitest run` or `npx vitest run`).
    - `test -f vitest.config.ts` succeeds.
    - `rg -n "environment:\\s*['\"]node['\"]" vitest.config.ts` OR `rg -n "environment:\\s*'node'" vitest.config.ts` matches.
    - `rg -n '\\*\\*\\/\\*\\.test\\.ts' vitest.config.ts` OR `rg -n "\\.test\\.ts" vitest.config.ts` matches an include pattern covering `src/**/*.test.ts`.
    - `npm run test:unit` exits 0 even when zero tests exist (allowed transiently before Task 2 adds tests) OR after Task 2 must exit 0 with tests — executor runs after Task 2 completes for final gate.
  </acceptance_criteria>
  <action>
    Run `npm install -D vitest@^4.1.4` (or equivalent) so `package-lock.json` updates. Add script `"test:unit": "vitest run"` (or `npx vitest run`) in package.json. Create `vitest.config.ts` using `defineConfig` from `vitest/config` with `test.environment: 'node'` and `include: ['src/**/*.test.ts']` (adjust if repo prefers `**/*.test.ts` but must pick up domain tests). Do not add Vitest to extension bundle — dev only. Ensure `npm run check-types` still passes (vitest types resolve via devDependency).
  </action>
  <verify>
    <automated>cd /Users/jakobrolik/programming/momentum/gamma/vscode-branch-cleaner && npm run test:unit</automated>
  </verify>
  <done>Vitest installed, config committed, test:unit script runs successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Domain types, candidates, baseline ordering, and unit tests</name>
  <files>src/domain/types.ts, src/domain/candidates.ts, src/domain/baseline.ts, src/domain/candidates.test.ts, src/domain/baseline.test.ts</files>
  <read_first>
    - .planning/phases/03-domain-candidates-baseline-merge/03-CONTEXT.md
    - src/git/branches.ts
  </read_first>
  <acceptance_criteria>
    - `rg -n "export (type|interface) ResolvedBaseline" src/domain/types.ts` matches.
    - `rg -n "BaselineResolutionError" src/domain/types.ts` matches (class or discriminated union with readonly `kind: 'no_baseline'` or similar stable sentinel).
    - `rg -n "function isAbandonedCandidate|export function isAbandonedCandidate" src/domain/candidates.ts` matches.
    - `rg -n "listCleanupCandidates" src/domain/candidates.ts` matches.
    - `rg -n "orderBaselineFallbacks|resolveBaselineFromOriginRemoteRefs" src/domain/baseline.ts` matches at least one exported ordering/resolution function implementing D-04 ladder on **plain** ref objects `{ name: string; commit?: string; type?: number }[]` or dedicated `RemoteRefSnapshot` type.
    - `rg -n "feature/zzz" src/domain/baseline.test.ts` OR fixture proving sorted `origin/*` tie-break OR equivalent lexicographic order assertion for third-tier fallback.
    - `rg -n "main" src/domain/baseline.test.ts` AND `rg -n "master" src/domain/baseline.test.ts` both match (fixtures for tier 1/2 after unusable HEAD).
    - `rg -n "HEAD" src/domain/baseline.test.ts` matches exclusion of `HEAD` from sorted origin/* tier.
    - `rg -n "current-feature" src/domain/candidates.test.ts` matches a fixture where branch name equals currentHeadName and expect **not** in candidate output.
    - `npm run test:unit` exits 0 with non-zero test count.
    - `npm run check-types` exits 0.
  </acceptance_criteria>
  <action>
    Implement **D-01**/**D-02**: `isAbandonedCandidate(branch: LocalBranchLike, _ctx?: unknown): boolean` and `listCleanupCandidates(branches: readonly LocalBranchLike[], currentHeadName: string | undefined): readonly LocalBranchLike[]` where locals are excluded when `branch.name === currentHeadName` (strict string equality; no normalization beyond Phase 2’s `name` field). Abandonment: no `upstream` OR missing/empty `upstream.commit` OR `upstream.remote !== 'origin'` (document in file comment as v1 default-remote scope). **D-04 ordering (pure):** implement resolution from an in-memory array of remote refs equivalent to `getRefs` output for `refs/remotes/origin/*`: (0) If `refs/remotes/origin/HEAD` exists and resolves via symref target name to another `refs/remotes/origin/<name>` entry with a commit, use that target as `ResolvedBaseline` (`ref: full ref string`, `displayLabel: e.g. origin/<short>`). (1) Else try `refs/remotes/origin/main`. (2) Else try `refs/remotes/origin/master`. (3) Else choose first `refs/remotes/origin/*` by **lexicographic sort of full ref name** among entries whose short name is not `HEAD` and which have a commit. (4) If none: throw or return `BaselineResolutionError` with message key like `cannot_resolve_default_branch` — **no** fallback to local `refs/heads/main`. Export `ResolvedBaseline` with at least `{ ref: string; displayLabel: string }`. Split helpers: `orderBaselineFallbacks` may only return ordered ref names for tests; `resolveBaselineFromOriginRemoteRefs` composes symref + fallbacks. Add Vitest fixtures proving: healthy upstream with commit on origin **excluded**; no upstream **included**; detached HEAD (`currentHeadName` undefined) still excludes nothing by name match only. Add baseline tests: symref HEAD → `origin/main` tip wins; missing HEAD falls through main→master→sorted; error when only unrelated remotes. Per **QUAL-01**, keep all assertions in `*.test.ts` beside sources.
  </action>
  <verify>
    <automated>cd /Users/jakobrolik/programming/momentum/gamma/vscode-branch-cleaner && npm run test:unit && npm run check-types</automated>
  </verify>
  <done>Domain pure layer and tests green; DOM-01/DOM-02 and baseline ordering slice of DOM-03 covered.</done>
</task>

</tasks>

<verification>
- `npm run test:unit` exits 0.
- `npm run check-types` exits 0.
- `rg -n "listCleanupCandidates" src/domain` shows implementation + tests.
</verification>

<success_criteria>
Vitest in place; pure domain for candidates + baseline ladder from ref snapshots tested; structured no-baseline error type exists for plan 03-02 adapter.
</success_criteria>

<output>
After completion, create `.planning/phases/03-domain-candidates-baseline-merge/03-01-SUMMARY.md` per execute-plan convention if required.
</output>
